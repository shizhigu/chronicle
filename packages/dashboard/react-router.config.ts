import type { Config } from '@react-router/dev/config';

export default {
  // SPA-less server-rendered mode with selective streaming
  ssr: true,
  // We'll enable future flags when v7 stabilizes
} satisfies Config;
